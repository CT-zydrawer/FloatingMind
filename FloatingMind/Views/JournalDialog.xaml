<Window x:Class="FloatingMind.JournalDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
        Title="审计日志 - Journal"
        Height="600" Width="850" ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource RegionBrush}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="{DynamicResource DarkRegionBrush}"
                Padding="12,10" CornerRadius="6" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <iconPacks:PackIconLucide Kind="ScrollText" Width="16" Height="16"
                                          Foreground="{DynamicResource PrimaryTextBrush}"
                                          VerticalAlignment="Center" />
                <TextBlock Text="审计日志" FontSize="15" FontWeight="Bold" Margin="6,0,0,0"
                           Foreground="{DynamicResource PrimaryTextBrush}" VerticalAlignment="Center" />
                <TextBlock x:Name="TxtCount" FontSize="12" Margin="12,0,0,0"
                           Foreground="{DynamicResource SecondaryTextBrush}" VerticalAlignment="Center" />
            </StackPanel>
        </Border>
        <ListView Grid.Row="1" x:Name="JournalList"
                  Background="Transparent"
                  BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"
                  Foreground="{DynamicResource PrimaryTextBrush}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="时间" Width="150"
                                    DisplayMemberBinding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" />
                    <GridViewColumn Header="事件" Width="120"
                                    DisplayMemberBinding="{Binding Event}" />
                    <GridViewColumn Header="Agent" Width="110"
                                    DisplayMemberBinding="{Binding Agent}" />
                    <GridViewColumn Header="目标" Width="200"
                                    DisplayMemberBinding="{Binding Target}" />
                    <GridViewColumn Header="详情" Width="230"
                                    DisplayMemberBinding="{Binding Details}" />
                </GridView>
            </ListView.View>
            
        </ListView>
    </Grid>
</Window>
